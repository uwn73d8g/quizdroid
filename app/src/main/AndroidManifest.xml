<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.washington.uwn73d8g.quizdroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".OverviewFragment1" />
        <activity
            android:name=".AnswerFragment1"
            android:label="@string/title_activity_answer_fragment1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".QuestionFragment1" />
        <activity android:name=".AnswerFragment2" />
        <activity android:name=".QuestionFragment2"></activity>
    </application>

</manifest>